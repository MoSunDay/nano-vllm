Commit: bb823b3e06983d71485a8e1f23715ebd87d98ef8

# nano-vllm 逻辑结构

轻量级 LLM 推理引擎，从零实现 vLLM 核心功能。纯 Python，约 1200 行，支持 prefix caching、tensor parallelism、CUDA graph、torch.compile 等优化。

## 模块索引

| 模块 | 职责 |
|------|------|
| [engine](agents/engine/index.md) | 调度、KV cache block 管理、序列生命周期、模型执行编排 |
| [layers](agents/layers/index.md) | 注意力、线性层、激活函数、RMSNorm、RoPE、采样器、embedding/LM head |
| [models](agents/models/index.md) | 具体模型实现（Qwen3） |
| [utils](agents/utils/index.md) | 全局上下文传递、权重加载 |

### Engine（引擎）— 餐厅的"总经理"

决定"谁先做、怎么做、用什么资源"的大管家。

想象一家繁忙的餐厅：

- **Scheduler（调度器）** = 前台领位员。客人来了（用户请求），领位员决定谁先进去吃、谁排队等位。餐厅分两种服务模式：
  - **Prefill（预填充）**：客人刚坐下，厨师要把整桌菜一次性上齐（把 prompt 所有 token 一次算完）
  - **Decode（解码）**：菜上完后，客人每要一道加菜就单独做一道（每步生成 1 个 token）
  - **为什么必须存在**：没有调度器，所有用户请求同时涌入 GPU。但 GPU 显存是有限的——每个请求都要占 KV cache，显存不够就 OOM（内存溢出），程序直接崩了。调度器像领位员控制餐厅流量：显存充足时放更多请求并行，显存紧张时让新请求排队。还要平衡 prefill 和 decode 两类任务的资源占用。
- **BlockManager（块管理器）** = 餐厅的储物柜管理员。厨师做菜时的半成品（KV cache）要存起来，柜子分成固定大小的格子（block，每个 256 个 token）。如果两桌客人点了相同的前菜，半成品可以**共用**，不用重复做——这就是 **prefix caching**。
  - **为什么必须存在**：没有块管理器，每个请求独占连续显存来存 KV cache——预分配了 2048 个 token 的空间但只用了 100 个，剩下 1948 个全空着被占着（浪费空间）；两个请求开头一样，各自的 KV cache 分别存了一份完全相同的内容（无法复用，重复计算）。块管理器把 KV cache 切成固定大小的块，用到多少就分配多少，相同前缀的块还能跨请求共享。
- **Sequence（序列）** = 一位客人从进门到吃完的整个生命周期。有三种状态：等位（WAITING）→ 用餐中（RUNNING）→ 吃完走人（FINISHED）。
  - **为什么必须存在**：没有 Sequence，调度器不知道一个请求当前处于什么状态——还在排队？正在跑？跑完了？也不知道它生成了多少 token、还剩多少预算、有没有遇到结束符。每个请求被包装成 Sequence 对象来追踪完整生命周期，调度器每轮扫描所有 Sequence 的状态来决定下一步做什么。
- **ModelRunner（模型执行器）** = 后厨的总厨师长。负责把领位员排好的单子转成具体的炒菜指令，然后指挥灶台（GPU）开火。
  - **为什么必须存在**：调度器说"这 8 个请求该跑了"，但这只是一句话。谁来把 8 个请求的数据拼成 GPU 能吃的 tensor？谁来调用模型的前向传播？谁来把输出转回每个请求对应的结果？ModelRunner 是调度器和 GPU 之间的"翻译官"——准备输入 tensor、调用模型前向传播、拆分输出、还负责 CUDA Graph 录制和 torch.compile 等性能优化。

### Layers（层）— 后厨的"标准菜谱和工具"

做菜用的基础工具和标准工序，可以反复复用。Layers 不是一道完整的菜，而是做任何菜都会用到的**基础操作**：

- **Attention（注意力）** = 厨师的"尝味"过程。做每道菜时，厨师要回头看之前所有步骤的味道（已生成的 token），决定现在该怎么调味。FlashAttention 就是让厨师能极快地"扫一遍"所有味道的高效技巧。
  - **为什么必须存在**：没有注意力，每个 token 是孤岛——"猫"不知道"鱼"的存在，"吃"不知道谁是主语谁是宾语。矩阵乘法是各算各的，词和词之间没有交互。注意力让每个词可以去"看"句子中所有其他词，并决定该关注谁。
- **Linear（线性层）** = 各种切菜工具。不同大小的刀、不同的切法：
  - `ColumnParallelLinear` = 把一块大豆腐竖着切，分给多个厨师同时切
  - `RowParallelLinear` = 横着切，切完再拼回去
  - `MergedColumnParallelLinear` = 把两种食材（gate + up）放在一块同时切，省一次起刀
  - **为什么必须存在**：线性层（矩阵乘法 `y = Wx`）是模型真正"学习"和"思考"的地方——`W` 是可训练参数，训练就是调整这些 `W`。没有矩阵乘法就没有深度学习。三种并行方式是因为单个 GPU 放不下太大模型，需要把矩阵拆到多张卡上协作计算。
- **RMSNorm（归一化）** = 调味标准化。不管食材本身咸淡如何，统一调到一个基准味道，防止味道越来越离谱。
  - **为什么必须存在**：数据经过一层又一层的矩阵乘法，如果乘以大于 1 的数，数值越来越大，几层之后变成 `inf`（爆炸）；如果乘以小于 1 的数，越来越小，几层之后变成 `0`（消失）。无论哪种，后面的层收到的都是垃圾数据，模型根本训练不动。RMSNorm 每过一层就把数据"拉回来"——太大了就缩小，太小了就放大，始终维持在一个合理的范围。
- **RoPE（旋转位置编码）** = 给每个食材贴"时间标签"。让模型知道"这个 token 是第几个出现的"。就像做菜的步骤顺序——先热油再下菜，顺序很重要。RoPE 用"旋转"的方式把位置信息编码到数据里。
  - **为什么必须存在**：矩阵乘法不关心顺序——"猫""吃""鱼"三个向量，打乱顺序算出来的结果是一样的。没有 RoPE，模型把"我爱你"和"你爱我"看成完全一样的东西，分不清主语和宾语。RoPE 用旋转给每个位置加不同的标记，而且旋转角度差天然编码了相对距离。
- **Activation（激活函数，SiLU）** = 食材的"化学反应"。生肉要通过烹饪（非线性变换）才能变成可吃的菜。SiLU 的作用就是给数据加一点非线性，让模型能学到复杂的模式。
  - **为什么必须存在**：矩阵乘法的组合还是矩阵乘法。100 层线性变换叠在一起，数学上等价于 1 层线性变换。模型再深也没有用——它只能学到输入输出之间的直线关系，永远无法学会"如果 A 且 B 则 C"这种非线性逻辑。SiLU 在矩阵乘法之间插入非线性，让每一层不再白叠。
- **Sampler（采样器）** = 最后的"选菜"环节。模型输出的是一道菜的所有可能口味及其概率（logits），采样器根据 temperature（温度）决定选哪种口味——温度高就大胆尝鲜，温度低就选最保险的。
  - **为什么必须存在**：模型输出的是一个概率分布——"下一个词有 60% 可能是'鱼'，30% 是'肉'……"，它给你一张菜单，但没有帮你点菜。没有采样器，模型只能输出"菜单"（概率），不能"上菜"（具体文字）。
- **Embedding & LM Head** = 翻译官：
  - **Embedding**：把人类的文字翻译成模型能理解的数字（文字 → 向量），就像把菜单翻译成厨房暗号
  - **LM Head**：反过来，把模型的数字输出翻译回文字（向量 → 词表概率），就像厨师把做好的菜描述成菜名
  - **为什么必须存在**：模型是数学运算器，只认识浮点数，不认识汉字。Embedding 是文字和数学之间的桥梁——用高维向量让模型自己学会哪些词真正相似（而不是被编号强加错误关系）。LM Head 则是反向翻译——模型的内部向量对人毫无意义，LM Head 把它映射到词表大小的 logits，再变成概率。没有 Embedding 模型无法开始工作，没有 LM Head 模型无法输出结果。

### Models（模型）— "具体菜系的完整菜单"

用 Layers 的工具，按照特定菜系的配方，组装出一套完整的菜。

如果 Layers 是"面粉、鸡蛋、烤箱"这些通用工具，那 Models 就是：

- **Qwen3** = 一家川菜馆的完整菜谱。规定了用哪些工具、按什么顺序、加什么调料来做出一道水煮鱼。
- 具体来说，`Qwen3ForCausalLM` 定义了：先用 Embedding 翻译文字 → 经过 N 层 Decoder（每层包含 Attention + MLP）→ 用 LM Head 翻译回来。

为什么要分层？因为换一家菜馆（比如换成 LLaMA），Layers 的工具不用变，只要换 Models 的"菜谱"就行。这就是为什么代码里说"扩展新模型只需新建一个文件"。

### Utils（工具）— 餐厅的"水电煤基础设施"

所有部门都依赖、但没人愿意自己管的基础服务。

- **Context（上下文）** = 餐厅的广播系统。总经理（ModelRunner）每轮通过广播告诉所有岗位"现在是 prefill 阶段""3 号桌的序列长度是 128"。各岗位（Attention、LM Head）只要竖起耳朵听就行，不用总经理挨个跑过去说——这就是**避免层层传参**。
- **Loader（加载器）** = 食材采购员。去仓库（safetensors 文件）取食材，按照菜单要求（`packed_modules_mapping`）把散装食材合并打包（如把 q/k/v 三个投影的权重合成一个），再按厨师人数分好份（TP 分片），送到每个灶台。

### 总结

`Utils` 是水电煤 → `Layers` 是工具箱 → `Models` 是菜谱 → `Engine` 是总经理。四层协作，从用户输入到生成输出，完成一次完整的 LLM 推理。

## 入口

- `nanovllm.LLM`（即 `LLMEngine`）是唯一的用户入口，提供 `generate()` 方法
- `nanovllm.SamplingParams` 控制 temperature、max_tokens、ignore_eos

## 外部依赖

torch, triton, transformers, flash-attn, xxhash, safetensors

## 功能索引

参见 [features/index.md](features/index.md)
